export function VideoContainer({ name, isMuted, isMainParticipant }: object): object;
export function Header({ title, participantCount }: object): object;
export function Controls({ actions }: object): object;
export function ParticipantsList({ participants }: object): object;
export function VideoChatPage(): object;
export default VideoChatPage;
