export function ParticipantTile({ name, avatarUrl, isSpeaking, muted }: object): object;
export function Header({ title, participantCount }: object): object;
export function FooterControls({ actions }: object): object;
export function VideoChatPage(): object;
export default VideoChatPage;
