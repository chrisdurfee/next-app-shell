/**
 * @type {Array<object>} MESSAGES_THREADS
 *
 * Each object represents a conversation thread with summary info and a `thread` array of message bubbles.
 */
export const MESSAGES_THREADS: Array<object>;
