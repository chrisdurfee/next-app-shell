export function WeekDayCell({ date, isToday, isOutsideMonth, currentDate, select }: {
    date: string;
    isToday: boolean;
    isOutsideMonth: boolean;
    currentDate: string;
    select: Function;
}): object;
export default WeekDayCell;
