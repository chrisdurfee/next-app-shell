/**
 * STEPS
 * Enumerates all possible steps in the onboarding process.
 */
export type STEPS = string;
export namespace STEPS {
    let WELCOME: string;
    let PROJECT_SETUP: string;
    let THEMING: string;
    let MODULES: string;
    let APP_SHELL: string;
    let CONGRATULATIONS: string;
}
